xAxis.default <- list(
  gridIndex = 0,
  position = "bottom",
  type = "category",
  name = "",
  nameLocation = "end",
  # min = "auto",
  # max = "auto",
  scale = FALSE,
  splitNumber = 5,
  # interval
  axisLine = list(
    show = TRUE,
    onZero = TRUE
  ),
  axisTick = list(
    show = TRUE,
    alignWithLabel = FALSE,
    interval = "auto"
  ),
  axisLabel = list(
    show = TRUE,
    interval = "auto",
    rotate = 0
  ),
  splitLine = list(
    show = TRUE,
    interval = "auto"
  ),
  splitArea = list(
    show = FALSE,
    interval = "auto"
  )
  # data = list()
)


yAxis.default <- list(
  gridIndex = 0,
  position = "left",
  type = "value",
  name = "",
  nameLocation = "end",
  # min = "auto",
  # max = "auto",
  scale = FALSE,
  splitNumber = 5,
  # interval
  axisLine = list(
    show = TRUE,
    onZero = TRUE
  ),
  axisTick = list(
    show = TRUE,
    alignWithLabel = FALSE,
    interval = "auto"
  ),
  axisLabel = list(
    show = TRUE,
    interval = "auto",
    rotate = 0
  ),
  splitLine = list(
    show = TRUE,
    interval = "auto"
  ),
  splitArea = list(
    show = FALSE,
    interval = "auto"
  )
  # data = list()
)
